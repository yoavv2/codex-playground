---
phase: 07-collaboration-mode-and-user-input-requests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/src/api/queryKeys.ts
  - farfield/apps/mobile/src/api/live-state.ts
  - farfield/apps/mobile/src/api/thread-actions.ts
  - farfield/apps/mobile/src/hooks/useThreadMutations.ts
  - farfield/apps/mobile/src/hooks/useThreadLiveState.ts
  - farfield/apps/mobile/src/hooks/useCollaborationModes.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - The mobile app has a typed read path for `/api/threads/:id/live-state` that can surface pending `request_user_input` prompts without screen-level schema parsing.
    - Collaboration mode options and thread live-state data are queryable through dedicated hooks keyed for safe invalidation.
    - User-input submission has a typed mutation path (`POST /api/threads/:id/user-input`) with predictable cache invalidation after success.
  artifacts:
    - farfield/apps/mobile/src/api/queryKeys.ts
    - farfield/apps/mobile/src/api/live-state.ts
    - farfield/apps/mobile/src/api/thread-actions.ts
    - farfield/apps/mobile/src/hooks/useThreadMutations.ts
    - farfield/apps/mobile/src/hooks/useThreadLiveState.ts
    - farfield/apps/mobile/src/hooks/useCollaborationModes.ts
  key_links:
    - farfield/apps/mobile/src/api/live-state.ts provides parsed live-state and pending-user-input mapping consumed by farfield/apps/mobile/src/hooks/useThreadLiveState.ts.
    - farfield/apps/mobile/src/api/thread-actions.ts exposes submit-user-input action used by farfield/apps/mobile/src/hooks/useThreadMutations.ts.
    - farfield/apps/mobile/src/api/queryKeys.ts defines shared keys used by read hooks, mutation invalidation, and live updates.
---

<objective>
Build the Phase 07 data/mutation foundation for collaboration mode and user-input prompt handling.

Purpose: Land typed API contracts and hooks first so UI work in 07-02/07-03 is straightforward and low-risk.
Output: New live-state and collaboration hooks plus submit-user-input mutation plumbing.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@farfield/apps/server/src/index.ts
@farfield/apps/server/src/http-schemas.ts
@farfield/packages/codex-protocol/src/thread.ts
@farfield/apps/mobile/src/api/queryKeys.ts
@farfield/apps/mobile/src/api/collaboration.ts
@farfield/apps/mobile/src/api/thread-actions.ts
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
</context>

<tasks>

<task type="auto">
  <name>Add typed live-state API surface and pending user-input extraction helpers</name>
  <files>farfield/apps/mobile/src/api/live-state.ts, farfield/apps/mobile/src/api/queryKeys.ts</files>
  <action>Create `live-state.ts` with a validated envelope for `GET /api/threads/:id/live-state` (`ok`, `threadId`, `ownerClientId`, `conversationState`, `liveStateError`). Export typed helper(s) that map `conversationState.requests` to pending `item/tool/requestUserInput` requests (including requestId and per-question metadata needed for rendering). Extend `queryKeys` with live-state keys and an explicit collaboration-mode list key so hooks and mutations share one cache contract.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Thread live-state can be queried and pending user-input requests can be consumed without ad-hoc parsing in screen files.</done>
</task>

<task type="auto">
  <name>Add submit-user-input transport and mutation invalidation wiring</name>
  <files>farfield/apps/mobile/src/api/thread-actions.ts, farfield/apps/mobile/src/hooks/useThreadMutations.ts</files>
  <action>Extend `thread-actions.ts` with typed `submitUserInput(threadId, body)` for `POST /api/threads/:id/user-input`, validating request/response with protocol-compatible schemas (`requestId`, `response`, optional `ownerClientId`). Add `useSubmitUserInput()` mutation hook that invalidates the thread detail and live-state keys for that thread after success so prompt state converges automatically.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>UI code can submit request_user_input answers through a single typed hook with automatic refetch behavior.</done>
</task>

<task type="auto">
  <name>Create dedicated read hooks for collaboration modes and thread live-state</name>
  <files>farfield/apps/mobile/src/hooks/useThreadLiveState.ts, farfield/apps/mobile/src/hooks/useCollaborationModes.ts</files>
  <action>Implement focused TanStack Query hooks around the new keys/modules: one for per-thread live-state (including pending user-input extraction) and one for available collaboration modes. Keep these hooks UI-agnostic: return data/loading/error/refetch primitives, no component-specific formatting. Ensure query enablement depends on a non-empty threadId.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Thread-level screens can import stable read hooks for both collaboration options and pending user-input state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] New hooks compile cleanly and expose typed return surfaces (no `any`).
- [ ] `submitUserInput` mutation invalidates the same thread’s detail/live-state keys (verified in code).
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mobile data layer has first-class support for live-state and pending user-input requests
- Collaboration mode and user-input operations are available through typed hooks/mutations
  </success_criteria>

<output>
After completion, create `.planning/phases/07-collaboration-mode-and-user-input-requests/07-01-SUMMARY.md`
</output>
