---
phase: 07-collaboration-mode-and-user-input-requests
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - farfield/apps/mobile/src/hooks/useThreadLiveState.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Pending `request_user_input` prompts are visible from mobile thread detail with enough question/option context to answer accurately.
    - Users can submit answers from the phone, and completed requests disappear or update after invalidation.
    - Phase 07 planning artifacts are closed with accurate roadmap/state handoff after implementation and verification.
  artifacts:
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - farfield/apps/mobile/src/hooks/useThreadLiveState.ts
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - farfield/apps/mobile/app/thread/[threadId].tsx renders pending prompts from farfield/apps/mobile/src/hooks/useThreadLiveState.ts.
    - Prompt submit actions in farfield/apps/mobile/app/thread/[threadId].tsx use `useSubmitUserInput()` and rely on query invalidation for consistency.
    - `.planning/STATE.md` and `.planning/ROADMAP.md` are updated only after successful manual verification of request-user-input flows.
---

<objective>
Finish Phase 07 by implementing mobile request_user_input handling and completing planning closeout.

Purpose: Let remote users respond to Codex user-input prompts directly from phone and formally hand off to Phase 08.
Output: Pending user-input prompt UI + submission flow in thread detail, plus summary/state/roadmap updates.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-collaboration-mode-and-user-input-requests/07-01-PLAN.md
@.planning/phases/07-collaboration-mode-and-user-input-requests/07-02-PLAN.md
@farfield/packages/codex-protocol/src/thread.ts
@farfield/apps/mobile/src/hooks/useThreadLiveState.ts
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Render pending user-input requests with question-level detail in thread detail</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx, farfield/apps/mobile/src/hooks/useThreadLiveState.ts</files>
  <action>Add a dedicated section for pending `item/tool/requestUserInput` requests sourced from live-state. For each request, render requestId plus question cards (header/question/options) with clear empty-state copy when none are pending. Keep this section distinct from approval cards to avoid mixing semantics.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>The thread screen consistently shows pending user-input prompts when they exist and a clear “none pending” state when they do not.</done>
</task>

<task type="auto">
  <name>Implement request answer capture and submission UX</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>For each pending request, implement local answer state keyed by question id and submit via `useSubmitUserInput()`. Support the option-based flow first (including explicit handling for `isOther` prompts via free-text input when needed), disable submit until required answers are present, and show per-request pending/error feedback. On success, clear local draft state for that request and rely on invalidation/refetch to remove completed prompts.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>User-input prompts can be answered from mobile with guarded validation and clear success/error feedback.</done>
</task>

<task type="auto">
  <name>Close Phase 07 planning artifacts after verification</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>After implementing and manually verifying Phase 07 behavior, create `07-03-SUMMARY.md` and update roadmap/state metadata: Phase 07 marked complete, plan/summary counts updated, and next action redirected to Phase 08. Keep prior history entries immutable and append only new factual completion notes.</action>
  <verify>`test -f .planning/phases/07-collaboration-mode-and-user-input-requests/07-03-SUMMARY.md`</verify>
  <done>Planning docs accurately record Phase 07 completion and provide clean Phase 08 handoff.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` compiles with user-input request UI.
- [ ] Manual smoke check against secured local Farfield verifies:
- [ ] A pending `request_user_input` prompt appears in thread detail with readable questions/options.
- [ ] Submitting answers succeeds and prompt state refreshes (resolved prompt disappears or marks completed).
- [ ] Existing thread actions (send, refresh, mode switching) still function while prompt UI is present.
- [ ] `.planning/phases/07-collaboration-mode-and-user-input-requests/07-03-SUMMARY.md` exists and planning docs are updated for Phase 07 completion.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mobile users can complete request_user_input flows without desktop intervention
- Phase 07 planning documentation is complete and points to the next roadmap phase
  </success_criteria>

<output>
After completion, create `.planning/phases/07-collaboration-mode-and-user-input-requests/07-03-SUMMARY.md`
</output>
