---
phase: 07-collaboration-mode-and-user-input-requests
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - farfield/apps/mobile/src/hooks/useThread.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Thread detail clearly shows the active collaboration mode and available mode options from the server.
    - Users can switch collaboration mode from the phone and see in-flight/success/error feedback without leaving the thread.
    - Mode changes refresh relevant thread data so the header reflects the latest server state.
  artifacts:
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - farfield/apps/mobile/src/hooks/useThread.ts
  key_links:
    - farfield/apps/mobile/app/thread/[threadId].tsx consumes `useCollaborationModes()` and `useSetCollaborationMode()` from the data layer added in 07-01.
    - Mode change success invalidates thread detail and collaboration mode queries through farfield/apps/mobile/src/hooks/useThreadMutations.ts.
    - Thread header mode display derives from thread detail (`latestCollaborationMode`) and server mode list, not hard-coded values.
---

<objective>
Implement the collaboration-mode control surface in thread detail on top of the 07-01 data foundation.

Purpose: Deliver the first half of Phase 07 user value by letting remote users inspect and switch mode from mobile.
Output: Mode read/write UI in thread detail with loading, error, and retry behavior.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-collaboration-mode-and-user-input-requests/07-01-PLAN.md
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/mobile/src/hooks/useThread.ts
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
@farfield/apps/mobile/src/hooks/useCollaborationModes.ts
@farfield/apps/mobile/src/api/collaboration.ts
@farfield/apps/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Add a collaboration mode section to thread detail header</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Extend the thread header card with a dedicated "Collaboration Mode" section showing current mode from `thread.latestCollaborationMode` and server-supported options from `useCollaborationModes()`. Keep fallback copy when no modes are available (agent unsupported) and preserve existing thread metadata and live-status chip layout.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>The current mode is always visible in thread detail with deterministic fallback states for unsupported/empty mode lists.</done>
</task>

<task type="auto">
  <name>Wire mode switching actions with per-selection pending and error feedback</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Render selectable controls (chips/buttons) for available modes and connect them to `useSetCollaborationMode()`. Disable only the active request path while mutation is pending, avoid duplicate sends, and surface actionable mutation errors inline in the mode section. Keep response handling local to mode controls; do not block message sending or pull-to-refresh while a mode mutation is running.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Users can change mode from the phone and understand pending/error states without losing normal thread interactions.</done>
</task>

<task type="auto">
  <name>Ensure thread hook and UI refresh semantics support mode updates cleanly</name>
  <files>farfield/apps/mobile/src/hooks/useThread.ts, farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Align thread-detail refresh behavior so mode updates are reflected quickly after mutation invalidation or SSE events. Keep existing `isLoading` vs `isRefreshing` semantics intact and do not regress conversation/approval rendering while adding mode controls.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>Mode changes converge into thread detail without introducing full-screen reload regressions in existing chat UX.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` compiles with collaboration mode controls.
- [ ] Manual smoke check against secured local Farfield confirms: changing `default`/`plan` succeeds and the thread header reflects the updated mode after refresh/invalidation.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Collaboration mode is visible and changeable in mobile thread detail
- Existing thread read/send/refresh flows remain stable while mode controls are active
  </success_criteria>

<output>
After completion, create `.planning/phases/07-collaboration-mode-and-user-input-requests/07-02-SUMMARY.md`
</output>
