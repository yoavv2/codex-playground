---
phase: 05-mvp-ui-threads-and-chat
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - farfield/apps/mobile/src/hooks/useThread.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - The thread detail route becomes a usable mobile chat surface instead of a read-only transcript page.
    - Sending a message from the phone is possible without regressing the current thread-detail read path.
    - Refetches and send mutations no longer force the user through a full-screen loading transition once thread data already exists.
  artifacts:
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - farfield/apps/mobile/src/hooks/useThread.ts
  key_links:
    - farfield/apps/mobile/src/hooks/useThread.ts exposes fetch state that lets farfield/apps/mobile/app/thread/[threadId].tsx differentiate first load from background refresh.
    - farfield/apps/mobile/app/thread/[threadId].tsx uses the existing `useSendMessage()` mutation hook and relies on shared invalidation instead of custom cache writes.
---

<objective>
Convert the thread detail route into an MVP chat surface with manual refresh and a working message composer.

Purpose: Establish the core read-and-send loop from the phone before layering on interrupt and approval controls.
Output: A thread detail screen that preserves the current conversation view, adds a composer, and supports non-blocking refresh behavior.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# No 05-CONTEXT.md exists yet; use the current Phase 04 read/write surfaces.
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/mobile/src/hooks/useThread.ts
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
@farfield/apps/mobile/src/api/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Expose interactive fetch state from the thread-detail hook</name>
  <files>farfield/apps/mobile/src/hooks/useThread.ts</files>
  <action>Keep `readThread()` plus pending-approval reads as the single data source, but return enough state for an interactive screen: preserve existing data during background refetches, expose a dedicated refresh signal for pull-to-refresh, and keep the current HTTP 400 pending-approval fallback so unsupported live-state reads do not block the main screen. Do not split this into multiple network hooks in the route component.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>The detail screen can refresh in place after first load instead of bouncing back to the global loading state.</done>
</task>

<task type="auto">
  <name>Reshape the detail route into a mobile-friendly chat layout</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Rework the screen from a static `ScrollView` transcript into an MVP chat surface: keep the existing header metadata, add manual refresh, improve the turn cards for scanability on mobile, and reserve stable bottom-space for message entry by using an appropriate keyboard-safe layout. Keep rendering intentionally simple: plain text only, with code-like content shown as monospaced text if needed. Do not add markdown rendering, syntax highlighting, or collaboration-mode controls in this plan.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>The screen remains readable on a phone while leaving room for an always-available composer.</done>
</task>

<task type="auto">
  <name>Wire the composer to the existing send-message mutation</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Add local composer state with a `TextInput` and send button backed by `useSendMessage()`. Trim and block empty submissions, disable repeat taps while the mutation is pending, clear the draft only after success, and surface inline sending/error feedback that is useful on a small screen. Rely on the existing invalidation behavior from `useThreadMutations.ts` plus manual refresh; do not build optimistic fake turns or custom cache patching.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>A configured device can send a message from the thread screen and then see the updated conversation after invalidation/refetch.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with the updated detail route compiled.
- [ ] Manual smoke check against a secured local Farfield server confirms a thread can be opened, refreshed in place, and sent a new message from the phone without losing existing thread content during refetch.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The thread detail route supports the basic remote "read thread, type reply, send" workflow
- Refresh state is differentiated from initial load so the screen remains stable after first render
- Message composition stays intentionally simple so Phase 05 lands before rendering polish work
  </success_criteria>

<output>
After completion, create `.planning/phases/05-mvp-ui-threads-and-chat/05-02-SUMMARY.md`
</output>
