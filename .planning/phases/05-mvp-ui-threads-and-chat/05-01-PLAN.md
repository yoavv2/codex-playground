---
phase: 05-mvp-ui-threads-and-chat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/app/(tabs)/threads.tsx
  - farfield/apps/mobile/src/hooks/useThreads.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - The Threads tab works as an MVP browse surface for real thread lists instead of a thin read-only placeholder.
    - Pull-to-refresh uses refetch state instead of dropping back to a full-screen loading state after the first successful load.
    - The screen communicates coarse connection health from existing REST query results without taking on Phase 06 SSE reconnect behavior.
  artifacts:
    - farfield/apps/mobile/app/(tabs)/threads.tsx
    - farfield/apps/mobile/src/hooks/useThreads.ts
  key_links:
    - farfield/apps/mobile/app/(tabs)/threads.tsx consumes richer derived fetch state from farfield/apps/mobile/src/hooks/useThreads.ts instead of duplicating query-state math in the screen.
    - The connection banner maps typed Farfield client errors to user-facing status copy while leaving transport behavior unchanged.
---

<objective>
Turn the Threads tab into a practical MVP browse surface with filtering, better refresh behavior, and request-state connection feedback.

Purpose: Make it easy to find the right thread before the user drops into the detail/chat experience.
Output: An upgraded Threads screen with local search, clearer list rows, and a compact connection banner driven by existing query state.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# No 05-CONTEXT.md exists yet; plan directly from the current codebase.
@farfield/apps/mobile/app/(tabs)/threads.tsx
@farfield/apps/mobile/src/hooks/useThreads.ts
@farfield/apps/mobile/src/api/errors.ts
@farfield/packages/codex-protocol/src/app-server.ts
</context>

<tasks>

<task type="auto">
  <name>Expand the thread-list hook for UI-ready fetch state</name>
  <files>farfield/apps/mobile/src/hooks/useThreads.ts</files>
  <action>Keep the existing `GET /api/threads` query contract, but expose screen-friendly derived state: a list sorted by `updatedAt` descending, a first-load flag distinct from background refetching, and a pull-to-refresh flag that stays true while a manual refetch is in flight. Preserve the current conservative retry policy and do not add pagination, SSE invalidation, or new transport concerns in this plan.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>The Threads screen can distinguish initial load from refresh and render a sorted list without re-implementing query-state math.</done>
</task>

<task type="auto">
  <name>Add local search and richer list rendering to the Threads tab</name>
  <files>farfield/apps/mobile/app/(tabs)/threads.tsx</files>
  <action>Upgrade the screen from a static list to an MVP browse surface: add a local text filter that matches thread title, id, and preview; keep the existing tap-through navigation; and show an agent/source badge by using `source` when present and defaulting to `codex` for the generated thread shape. Split empty states between "no threads on the server" and "no results for this filter". Keep implementation in plain React Native primitives plus `StyleSheet`; do not introduce a component library or cross-screen design system in this phase.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>The user can narrow a long thread list locally and still open the correct thread from filtered results.</done>
</task>

<task type="auto">
  <name>Show coarse connection feedback without premature live-state work</name>
  <files>farfield/apps/mobile/app/(tabs)/threads.tsx</files>
  <action>Render a compact status banner above the list that maps current query state to simple REST-derived states: connected after a successful fetch, auth failed for `UnauthorizedError`, server unreachable/timeout for network errors, and "configure server" for `NoServerUrlError`. Do not wire `useFarfieldEvents()` yet and do not promise a reconnecting state machine here; Phase 06 owns live connection lifecycle and retry semantics.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>The Threads tab communicates whether the current server configuration is usable without expanding scope into SSE lifecycle behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with the upgraded Threads screen compiled.
- [ ] Manual smoke check against a secured local Farfield server confirms search/filter works, pull-to-refresh uses a non-blocking spinner, and the connection banner shows sensible copy for success plus at least one failure case.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Threads tab is useful for browsing a real thread list before entering detail view
- Filtered results and empty states behave predictably for both "no data" and "no matches"
- Connection feedback stays intentionally REST-based so Phase 06 can own SSE/reconnect behavior cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/05-mvp-ui-threads-and-chat/05-01-SUMMARY.md`
</output>
