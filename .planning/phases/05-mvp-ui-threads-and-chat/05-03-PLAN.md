---
phase: 05-mvp-ui-threads-and-chat
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - A user can interrupt a running thread from the mobile detail screen.
    - Pending approval requests become actionable from the phone for command, file-change, and apply-patch flows.
    - Phase 05 finishes with planning docs pointing the next execution step to Phase 06.
  artifacts:
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - farfield/apps/mobile/app/thread/[threadId].tsx uses `useInterruptThread()` and `useRespondToApproval()` from farfield/apps/mobile/src/hooks/useThreadMutations.ts for all new controls.
    - The approval and interrupt controls rely on existing query invalidation rules so thread detail and pending approvals refresh without custom cache writes.
    - The planning docs mark Phase 05 complete only after the UI controls and manual verification land.
---

<objective>
Finish the Phase 05 MVP by adding interrupt and approval actions to the thread detail screen, then close the phase with a clean handoff.

Purpose: Complete the "read, send, interrupt, approve" remote-control loop that defines the Milestone 2 MVP.
Output: Actionable interrupt and pending-approval controls in the thread UI, plus updated planning state that points the project at Phase 06.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/mobile/src/hooks/useThread.ts
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
@farfield/apps/mobile/src/api/approvals.ts
</context>

<tasks>

<task type="auto">
  <name>Add an interrupt control that respects running-thread state</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Render an interrupt action only when the thread has a meaningful in-progress state to stop (for example, the latest turn is `inProgress`). Wire it to `useInterruptThread()`, show pending/error feedback inline, and prevent duplicate taps while the mutation is running. Keep the UX lightweight and reversible: no modal workflow or multi-step confirmation unless the existing screen already needs one for clarity.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>A running Codex thread can be interrupted from the phone, and the UI reflects the in-flight request clearly.</done>
</task>

<task type="auto">
  <name>Replace read-only approval cards with actionable approve/deny controls</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Promote pending approvals into a high-visibility section near the top of the detail screen whenever `pendingApprovals.length > 0`. For each request, keep the current summary/detail rendering but add explicit Approve and Deny actions backed by `useRespondToApproval()`. Track pending state per approval request so one submission does not disable unrelated approvals, and keep the copy understandable for all three MVP approval types (`command`, `file-change`, `apply-patch`). Do not build a full diff viewer or generic tool-request framework in this phase.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>The user can resolve a pending approval from the phone, and the list refreshes to reflect the resolved state.</done>
</task>

<task type="auto">
  <name>Close Phase 05 with summary and planning handoff</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>After the UI work is complete and manually verified, create `05-03-SUMMARY.md`, update `.planning/STATE.md` to mark Phase 05 complete, and update `.planning/ROADMAP.md` so Phase 05 shows the correct plan/summary counts and the next action points at Phase 06. Keep the history entry concise and factual; do not retroactively rewrite earlier phase notes.</action>
  <verify>`test -f .planning/phases/05-mvp-ui-threads-and-chat/05-03-SUMMARY.md`</verify>
  <done>The project planning docs accurately show that Phase 05 is complete and that Phase 06 is now the next execution target.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with interrupt and approval actions compiled.
- [ ] Manual smoke check against a secured local Farfield server confirms: a running thread can be interrupted; a pending command/file-change/apply-patch approval can be approved or denied; the thread detail screen refreshes to the post-action state.
- [ ] `.planning/phases/05-mvp-ui-threads-and-chat/05-03-SUMMARY.md` exists, and `.planning/STATE.md` plus `.planning/ROADMAP.md` reflect Phase 05 completion.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The mobile app can complete the core MVP interrupt and approval workflows from the thread screen
- Approval actions avoid duplicate submissions and keep per-request status understandable
- The planning docs clearly hand off to Phase 06 after the final Phase 05 slice lands
  </success_criteria>

<output>
After completion, create `.planning/phases/05-mvp-ui-threads-and-chat/05-03-SUMMARY.md`
</output>
