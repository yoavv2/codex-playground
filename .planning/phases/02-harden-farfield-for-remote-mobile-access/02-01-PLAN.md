# Phase 02-01 Plan: Remote Security Hardening Baseline

## objective

Harden the Farfield server for remote mobile access by implementing the Phase 02 required security baseline in the target repo at `/Users/yoavhevroni/Documents/dev/farfield`:

- bearer token auth for protected HTTP APIs
- auth protection for `/events`
- CORS allowlist for remote use (no wildcard)
- debug API gating for remote use
- docs and verification commands for safe Tailscale usage

Preserve current local development behavior when no auth token is configured.

## execution_context

### Planning repo (this repo)

- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/PROJECT.md`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/ROADMAP.md`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/STATE.md`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/01-prep-and-decisions/01-CONTEXT.md`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md` (if present)

### Target implementation repo (Farfield)

- `/Users/yoavhevroni/Documents/dev/farfield/AGENTS.md` (must read before code changes)
- `/Users/yoavhevroni/Documents/dev/farfield/README.md`
- `/Users/yoavhevroni/Documents/dev/farfield/apps/server/src/index.ts`
- `/Users/yoavhevroni/Documents/dev/farfield/apps/server/src/http-schemas.ts`
- `/Users/yoavhevroni/Documents/dev/farfield/apps/server/test/http-schemas.test.ts`
- `/Users/yoavhevroni/Documents/dev/farfield/apps/server/test/*` (existing test patterns)
- `/Users/yoavhevroni/Documents/dev/farfield/scripts/dev.mjs` (remote run behavior)

### Discovery level

- `Level 2` (planning docs + local target repo inspection)
- No internet browsing required for this plan

### Execution mode assumptions

- Default to workspace `yolo` behavior unless `/gsd:execute-plan` override is provided
- This plan intentionally includes **verify checkpoints** for segmented execution

### Multi-repo commit note

This plan will modify files in:
- the Farfield repo (`/Users/yoavhevroni/Documents/dev/farfield`) for code/docs/tests
- the planning repo (`/Users/yoavhevroni/Documents/dev/codex-playground`) for `02-CONTEXT.md`, `STATE.md`, and summary

Stage files explicitly per repo. Do not use broad `git add` commands.

## context

### Phase metadata

- GSD phase: `02`
- Plan: `02-01`
- Phase name: `Harden Farfield for Remote Mobile Access`
- Target repo: `/Users/yoavhevroni/Documents/dev/farfield` (`main`)

### Verified target repo facts (from Phase 01)

- Server entrypoint exists at `apps/server/src/index.ts`
- `GET /events` exists and is currently unauthenticated
- `/api/*` routes exist and are currently unauthenticated
- `/api/debug/*` routes exist and are currently accessible
- JSON and SSE responses currently use wildcard CORS (`Access-Control-Allow-Origin: *`)
- `README.md` warns `dev:remote` is unauthenticated

### Required Phase 02 baseline scope (must land in this plan)

- `FARFIELD_AUTH_TOKEN`-driven auth for protected routes
- `/events` auth protection (compatibility path for query token only if needed)
- `FARFIELD_ALLOWED_ORIGINS`-driven allowlist CORS in remote use
- `FARFIELD_ENABLE_DEBUG_API` gating for `/api/debug/*`
- docs + curl verification instructions

### Deferred / not in this plan

- Final approval prompt API exposure decision and implementation (handled in `02-02-PLAN.md`)
- Mobile client changes
- SSE library selection (Phase 03)

## tasks

### Task 1: Security design discovery and branch prep

1. Read `/Users/yoavhevroni/Documents/dev/farfield/AGENTS.md` and follow its strict typing/Zod rules.
2. Inspect current request handling in `apps/server/src/index.ts` and identify the best insertion points for:
   - auth checks
   - CORS/preflight handling
   - debug route gating
3. Decide and record the route protection matrix (what is protected and when):
   - protect `/api/*` by default when token is configured
   - protect `/events` by default when token is configured
   - decide `/api/health` auth behavior for this plan (`FARFIELD_REQUIRE_AUTH_FOR_HEALTH` support or explicit temporary behavior)
4. (Recommended) create a feature branch in the Farfield repo before changes using the required prefix:
   - example: `codex/phase-02-remote-hardening`
5. Create/update `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md` with the chosen protection matrix and branch choice.

Deliverable:
- Documented protection matrix + branch choice in `02-CONTEXT.md`

### Task 2: Implement auth + CORS + debug gating in Farfield server

Implement the required remote hardening in `/Users/yoavhevroni/Documents/dev/farfield/apps/server/src/index.ts` (and helper files if extraction improves clarity/testability).

1. Add env-driven configuration parsing for:
   - `FARFIELD_AUTH_TOKEN`
   - `FARFIELD_ALLOWED_ORIGINS`
   - `FARFIELD_ENABLE_DEBUG_API`
   - optional `FARFIELD_REQUIRE_AUTH_FOR_HEALTH`
2. Introduce a small, deterministic request security layer (in `index.ts` or extracted helper module) that:
   - validates bearer auth for protected routes
   - supports a compatibility query token only for `/events` if required by the client path
   - applies CORS allowlist policy to JSON responses, SSE responses, and `OPTIONS`
   - includes `authorization` in `Access-Control-Allow-Headers` when auth is enabled
3. Remove wildcard CORS behavior for remote allowlisted usage while preserving local development usability.
4. Gate `/api/debug/*` using `FARFIELD_ENABLE_DEBUG_API` policy.
   - default to disabled for remote use
   - keep local development usage practical
5. Keep response schema behavior and route semantics unchanged unless required by the hardening work.

Deliverable:
- Hardened server route handling in Farfield codebase

### Verify Checkpoint A (security implementation)

Before continuing to docs/tests polish, run focused checks in `/Users/yoavhevroni/Documents/dev/farfield`:

- `bun run --filter @farfield/server test`
- `bun run --filter @farfield/server typecheck`
- `bun run --filter @farfield/server lint`

If any check fails, fix before proceeding.

### Task 3: Add tests for security policy behavior

1. Add focused tests for the new security behavior.
   - Prefer extracted pure helpers + unit tests (for example a new `apps/server/test/*security*.test.ts`)
   - Cover auth decisions, CORS allowlist decisions, and debug gating decisions
2. If request body schemas change, extend `apps/server/test/http-schemas.test.ts`.
3. Keep tests narrow and deterministic (no external network calls).

Deliverable:
- Passing test coverage for the new security policy logic

### Task 4: Update docs and produce manual verification commands

1. Update `/Users/yoavhevroni/Documents/dev/farfield/README.md`:
   - explain new auth env vars and remote security expectations
   - provide Tailscale-oriented startup guidance
   - document debug API gating behavior
2. Add manual verification commands (curl examples) for:
   - protected API without token -> `401`
   - protected API with token -> success
   - `/events` auth behavior
   - debug route blocked when disabled
3. Record exact verification commands/results in:
   - `02-CONTEXT.md` (Phase 02 notes)
   - execution summary during `/gsd:execute-plan`

Deliverable:
- README remote-hardening documentation + reproducible verification commands

### Verify Checkpoint B (end-to-end hardening validation)

Run the documented manual checks against a local Farfield dev run configured for remote use and confirm:

- auth works for protected routes
- CORS headers reflect the configured allowlist
- debug routes are blocked when disabled
- local-dev behavior remains usable when no token is configured

Document any deviations in `02-CONTEXT.md` and the plan summary.

### Task 5: Update planning artifacts and handoff to 02-02

1. Update `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md` with:
   - implemented env vars and route policy
   - `/api/health` auth decision outcome
   - any compatibility choices for `/events`
   - known limitations / follow-up items
2. Update `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/STATE.md` and roadmap snapshot counts/status as appropriate.
3. Prepare a short handoff section for `02-02-PLAN.md` (approval prompt API exposure) with notes about auth/CORS assumptions now in place.

Deliverable:
- Phase 02 context/state updated for the next plan in this phase

## verification

### Code + tests (Farfield repo)

- `bun run --filter @farfield/server test`
- `bun run --filter @farfield/server typecheck`
- `bun run --filter @farfield/server lint`

### Manual route checks (Farfield repo running locally)

- unauthenticated request to a protected `/api/*` route returns `401`
- authenticated request succeeds
- unauthenticated `/events` is rejected when auth is enabled
- authenticated `/events` connects successfully
- `/api/debug/*` is blocked when `FARFIELD_ENABLE_DEBUG_API=false`
- allowed origin gets correct CORS headers; disallowed origin is not allowed

### Planning artifacts (codex-playground repo)

- `test -f .planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md`
- `rg -n "FARFIELD_AUTH_TOKEN|FARFIELD_ALLOWED_ORIGINS|FARFIELD_ENABLE_DEBUG_API" .planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md`

## success_criteria

- [ ] Farfield server enforces token auth on protected routes when configured
- [ ] `/events` is protected under the same auth policy
- [ ] Remote CORS allowlist is enforced without wildcard origins
- [ ] Debug API routes are gated by config for remote use
- [ ] README documents secure Tailscale-based remote usage and env vars
- [ ] Focused Farfield server checks pass (test/typecheck/lint)
- [ ] `02-CONTEXT.md` records hardening decisions and outcomes for `02-02`

## output

Primary output from this planning command:
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-01-PLAN.md`

Expected outputs when executing this plan:
- Farfield code/docs/test changes in `/Users/yoavhevroni/Documents/dev/farfield/...`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md`
- `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/phases/02-harden-farfield-for-remote-mobile-access/02-01-SUMMARY.md`
- updated `/Users/yoavhevroni/Documents/dev/codex-playground/.planning/STATE.md` (and roadmap counts/status if changed)
