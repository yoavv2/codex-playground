---
phase: 04-build-typed-mobile-api-client
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/package.json
  - farfield/bun.lock
  - farfield/apps/mobile/metro.config.js
  - farfield/apps/mobile/app/_layout.tsx
  - farfield/apps/mobile/src/api/client.ts
  - farfield/apps/mobile/src/api/errors.ts
  - farfield/apps/mobile/src/api/queryKeys.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Expo Router is wrapped in a QueryClientProvider so Phase 04 and Phase 05 hooks can run without extra root surgery.
    - All Farfield HTTP calls go through one authenticated fetch helper that trims the configured base URL, applies Bearer auth, and normalizes transport, HTTP, and schema failures.
    - The Expo app can import @farfield/protocol from the workspace without Metro resolution failures.
  artifacts:
    - farfield/apps/mobile/src/api/client.ts
    - farfield/apps/mobile/src/api/errors.ts
    - farfield/apps/mobile/src/api/queryKeys.ts
    - farfield/apps/mobile/metro.config.js
  key_links:
    - farfield/apps/mobile/app/_layout.tsx provides a shared QueryClient to the router tree.
    - farfield/apps/mobile/src/api/client.ts reads serverUrl and authToken from src/settings/loadSettings().
    - farfield/apps/mobile/package.json declares @tanstack/react-query and @farfield/protocol for the mobile workspace.
---

<objective>
Establish the shared transport and query foundation for Phase 04 before any endpoint-specific client code lands.

Purpose: Remove one-off fetch logic and create one stable place for auth, runtime validation, cache keys, and workspace package resolution.
Output: Query provider wiring, Metro workspace config, and reusable API client primitives under `farfield/apps/mobile/src/api/`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-create-expo-app-skeleton/03-03-SUMMARY.md
@.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md
@farfield/apps/mobile/package.json
@farfield/apps/mobile/app/_layout.tsx
@farfield/apps/mobile/src/api/health.ts
@farfield/apps/mobile/src/settings/storage.ts
@farfield/packages/codex-protocol/src/app-server.ts
</context>

<tasks>

<task type="auto">
  <name>Install shared client dependencies and make Metro resolve workspace protocol code</name>
  <files>farfield/apps/mobile/package.json, farfield/bun.lock, farfield/apps/mobile/metro.config.js</files>
  <action>Add `@tanstack/react-query` and `@farfield/protocol` to `@farfield/mobile`. Create `metro.config.js` for Expo SDK 53 that watches the monorepo root and resolves the shared workspace package cleanly. Keep the config minimal: only add the watchFolders/resolver behavior needed for `@farfield/protocol` imports so the mobile app stays aligned with the existing Farfield workspace.</action>
  <verify>In `/Users/yoavhevroni/Documents/dev/codex-playground/farfield`, run `bun install`, then `bun run --filter @farfield/protocol build`.</verify>
  <done>`@farfield/mobile` can import `@farfield/protocol` during typecheck and Metro startup without module-resolution errors.</done>
</task>

<task type="auto">
  <name>Create a reusable authenticated Farfield transport layer</name>
  <files>farfield/apps/mobile/src/api/client.ts, farfield/apps/mobile/src/api/errors.ts</files>
  <action>Build a shared client module that loads connection settings from `src/settings`, rejects missing/blank `serverUrl`, applies `Authorization: Bearer ...` when `authToken` exists, and exposes a schema-aware `fetchJson()` helper. Normalize error handling into explicit client error types for unauthorized, unreachable server, timeout, non-2xx HTTP, and schema mismatch cases so all later endpoint modules surface consistent failures.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Endpoint modules can call one helper instead of re-implementing fetch, auth headers, URL cleanup, and response parsing.</done>
</task>

<task type="auto">
  <name>Wire the root app tree for shared query state and stable cache keys</name>
  <files>farfield/apps/mobile/app/_layout.tsx, farfield/apps/mobile/src/api/queryKeys.ts</files>
  <action>Wrap the root Expo Router stack in a `QueryClientProvider` with conservative defaults suited to a remote-control app (no aggressive background refetching, predictable retry behavior). Add a small query-key factory module for threads, thread detail, pending approvals, agents, and collaboration modes so later plans can share invalidation and avoid string drift.</action>
  <verify>`bun run --filter @farfield/mobile lint && bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>The app boots with a shared QueryClient and Phase 04 endpoint hooks have a single, pre-agreed cache key surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun install` completes after adding the new mobile dependencies.
- [ ] `bun run --filter @farfield/protocol build` passes so the shared protocol package exports are present.
- [ ] `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts without Metro resolution errors for `@farfield/protocol`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The mobile app has one shared, authenticated client primitive instead of endpoint-local fetch code
- QueryClientProvider is installed once at the root and ready for downstream hooks
- Workspace protocol imports are unblocked for Expo development
  </success_criteria>

<output>
After completion, create `.planning/phases/04-build-typed-mobile-api-client/04-01-SUMMARY.md`
</output>
