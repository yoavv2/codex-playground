---
phase: 04-build-typed-mobile-api-client
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - farfield/apps/mobile/src/api/messages.ts
  - farfield/apps/mobile/src/api/thread-actions.ts
  - farfield/apps/mobile/src/api/events.ts
  - farfield/apps/mobile/src/hooks/useThreadMutations.ts
  - farfield/apps/mobile/src/hooks/useFarfieldEvents.ts
  - farfield/apps/mobile/src/api/client.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Every write-side endpoint needed for the MVP UI has a typed mobile client method before Phase 05 starts wiring controls.
    - The mobile app has a reusable `/events` subscription helper that authenticates correctly but does not yet own reconnect UI behavior.
    - Mutation helpers invalidate the shared read query keys so Phase 05 can add controls without reworking cache semantics.
  artifacts:
    - farfield/apps/mobile/src/api/messages.ts
    - farfield/apps/mobile/src/api/thread-actions.ts
    - farfield/apps/mobile/src/api/events.ts
    - farfield/apps/mobile/src/hooks/useThreadMutations.ts
    - farfield/apps/mobile/src/hooks/useFarfieldEvents.ts
  key_links:
    - farfield/apps/mobile/src/hooks/useThreadMutations.ts uses query keys from 04-01 to invalidate thread reads after writes.
    - farfield/apps/mobile/src/api/events.ts reuses the shared config/auth primitives from the base client.
    - The write-side surface covers send-message, interrupt, collaboration mode changes, and approval responses expected by Phase 05.
---

<objective>
Finish the Phase 04 client surface by covering write operations and adding the low-level SSE subscription primitive.

Purpose: Hand Phase 05 a complete, typed client API so UI work can focus on interaction design instead of transport and protocol plumbing.
Output: Mutation client modules, mutation hooks with cache invalidation, and a reusable `subscribeEvents()` wrapper for `/events`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md
@farfield/apps/server/src/index.ts
@farfield/apps/server/src/http-schemas.ts
@farfield/apps/server/src/approvals.ts
@farfield/apps/mobile/src/api/health.ts
@farfield/apps/mobile/src/settings/storage.ts
@farfield/packages/codex-protocol/src/thread.ts
</context>

<tasks>

<task type="auto">
  <name>Create typed write-side client modules for all MVP thread actions</name>
  <files>farfield/apps/mobile/src/api/messages.ts, farfield/apps/mobile/src/api/thread-actions.ts, farfield/apps/mobile/src/api/client.ts</files>
  <action>Implement validated client methods for `POST /api/threads/:id/messages`, `POST /api/threads/:id/interrupt`, `POST /api/threads/:id/collaboration-mode`, and `POST /api/threads/:id/pending-approvals/respond`. Mirror the request/response contracts in `apps/server/src/http-schemas.ts` and `apps/server/src/index.ts`, including `ownerClientId` passthrough and the Phase 02 approval decision mapping expectations. Extend the shared client only where necessary for POST helpers and body serialization; keep transport concerns centralized instead of duplicating fetch logic in each module.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>The mobile app has one typed function per MVP write endpoint, and each one validates server envelopes before returning.</done>
</task>

<task type="auto">
  <name>Expose mutation hooks with predictable cache invalidation</name>
  <files>farfield/apps/mobile/src/hooks/useThreadMutations.ts</files>
  <action>Wrap the write-side client methods in TanStack Query mutations that invalidate or refresh the shared thread, thread-detail, and pending-approval query keys after success. Make the hook surface Phase 05-friendly: clear argument types, explicit loading/error state, and no UI-specific assumptions. Do not add buttons to screens yet; this plan prepares the interaction layer, it does not own the UI.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Phase 05 can import one hook module for send, interrupt, mode switch, and approval response actions without reinventing cache behavior.</done>
</task>

<task type="auto">
  <name>Implement the first reusable `/events` subscription helper</name>
  <files>farfield/apps/mobile/src/api/events.ts, farfield/apps/mobile/src/hooks/useFarfieldEvents.ts</files>
  <action>Build a low-level `subscribeEvents()` helper around `react-native-sse` that targets `/events`, prefers the `Authorization` header, and falls back to `access_token` query-param transport only if the library cannot send custom headers on the active platform. Parse incoming messages defensively, expose subscribe/unsubscribe cleanup, and keep reconnect policy intentionally basic because the robust reconnect strategy belongs to Phase 06. Add a thin hook wrapper only if it helps future UI consumers attach listeners safely.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>The codebase has one shared SSE primitive ready for later invalidation/live-state wiring, with auth handled in one place.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/protocol build` passes.
- [ ] `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with the new mutation and SSE modules compiled.
- [ ] Manual API smoke checks against a secured local Farfield server confirm send-message, interrupt, collaboration-mode, and approval-response calls succeed with a valid token and fail cleanly without one.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The write-side Phase 05 API surface exists before UI controls are added
- Query invalidation rules are defined once and reused by mutation hooks
- A shared authenticated `/events` helper exists without prematurely taking on Phase 06 reconnect complexity
  </success_criteria>

<output>
After completion, create `.planning/phases/04-build-typed-mobile-api-client/04-03-SUMMARY.md`
</output>
