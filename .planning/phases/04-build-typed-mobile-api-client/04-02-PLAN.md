---
phase: 04-build-typed-mobile-api-client
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - farfield/apps/mobile/src/api/threads.ts
  - farfield/apps/mobile/src/api/approvals.ts
  - farfield/apps/mobile/src/api/agents.ts
  - farfield/apps/mobile/src/api/collaboration.ts
  - farfield/apps/mobile/src/hooks/useThreads.ts
  - farfield/apps/mobile/src/hooks/useThread.ts
  - farfield/apps/mobile/app/(tabs)/threads.tsx
  - farfield/apps/mobile/app/thread/[threadId].tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - The Threads tab fetches real thread data from the configured Farfield server instead of hard-coded placeholder rows.
    - The Thread Detail screen reads the actual `GET /api/threads/:id` response and surfaces pending approvals from the dedicated Phase 02 endpoint.
    - Read-side responses are runtime-validated with shared protocol schemas plus local envelope schemas before UI code consumes them.
  artifacts:
    - farfield/apps/mobile/src/api/threads.ts
    - farfield/apps/mobile/src/api/approvals.ts
    - farfield/apps/mobile/src/hooks/useThreads.ts
    - farfield/apps/mobile/src/hooks/useThread.ts
  key_links:
    - farfield/apps/mobile/src/hooks/useThreads.ts uses the shared client and query keys from 04-01.
    - farfield/apps/mobile/app/(tabs)/threads.tsx renders query state from useThreads().
    - farfield/apps/mobile/app/thread/[threadId].tsx combines thread detail and pending approval reads for the selected route param.
---

<objective>
Implement the read-side typed API client for the first mobile surfaces and replace Phase 03 placeholder data with real server-backed reads.

Purpose: Prove the authenticated client works end-to-end for browsing threads before Phase 05 adds richer interaction controls.
Output: Validated read endpoints, TanStack Query hooks, and read-only Threads/Thread Detail screens backed by a live Farfield server.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-create-expo-app-skeleton/03-03-SUMMARY.md
@.planning/phases/02-harden-farfield-for-remote-mobile-access/02-CONTEXT.md
@farfield/apps/server/src/index.ts
@farfield/apps/server/src/approvals.ts
@farfield/apps/mobile/app/(tabs)/threads.tsx
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/packages/codex-protocol/src/app-server.ts
@farfield/packages/codex-protocol/src/thread.ts
</context>

<tasks>

<task type="auto">
  <name>Build validated read endpoints for threads, approvals, agents, and collaboration modes</name>
  <files>farfield/apps/mobile/src/api/threads.ts, farfield/apps/mobile/src/api/approvals.ts, farfield/apps/mobile/src/api/agents.ts, farfield/apps/mobile/src/api/collaboration.ts</files>
  <action>Create read-side client modules that call the actual Farfield contracts now present in `apps/server/src/index.ts`: `GET /api/threads`, `GET /api/threads/:id`, `GET /api/threads/:id/pending-approvals`, `GET /api/agents`, and `GET /api/collaboration-modes`. Reuse `@farfield/protocol` parsers for inner payloads where they fit, then wrap them in local Zod schemas for the server-specific `{ ok: true, ... }` envelopes, `agentId`, `threadId`, and `ownerClientId` metadata. Do not preserve the stale `GET /api/threads/:id/messages` assumption from the placeholder comment; the real read contract is `GET /api/threads/:id`.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Every read endpoint needed for thread browsing is represented by one typed function that either returns validated data or throws a normalized client error.</done>
</task>

<task type="auto">
  <name>Create query hooks for list and detail reads</name>
  <files>farfield/apps/mobile/src/hooks/useThreads.ts, farfield/apps/mobile/src/hooks/useThread.ts</files>
  <action>Layer TanStack Query hooks on top of the new read modules. Use the shared query keys from 04-01, keep retries conservative for auth/network failures, and return enough derived state for screens to render loading, empty, and error states without duplicating query logic. Include pending approvals in the thread-detail hook surface so the detail screen only orchestrates one read composition point.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Threads and thread detail UI can consume one hook each instead of issuing fetches inline.</done>
</task>

<task type="auto">
  <name>Replace placeholder thread UI with live read-only data</name>
  <files>farfield/apps/mobile/app/(tabs)/threads.tsx, farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Swap the hard-coded placeholder list for the real thread query. Render a lightweight but useful read-only thread detail view from the validated `GET /api/threads/:id` payload, including message/history content that already exists in the response and a pending-approvals section populated from the dedicated approvals endpoint. Keep this slice intentionally read-only: no composer, interrupt button, or approve/deny buttons yet, because those interaction controls belong to Phase 05.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>With valid settings pointed at a running Farfield instance, the app can browse real threads and inspect a real thread detail screen without placeholder copy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/protocol build` passes.
- [ ] `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with the new hooks/screens compiled.
- [ ] Manual smoke check against a secured local Farfield server shows the Threads tab listing real threads and the Thread Detail screen rendering live data plus pending approvals (read-only).
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Placeholder thread rows are gone from the Threads tab
- Thread detail uses the real `GET /api/threads/:id` contract and no longer documents a nonexistent `/messages` read endpoint
- Pending approvals are visible from the dedicated Phase 02 endpoint without adding action buttons yet
  </success_criteria>

<output>
After completion, create `.planning/phases/04-build-typed-mobile-api-client/04-02-SUMMARY.md`
</output>
