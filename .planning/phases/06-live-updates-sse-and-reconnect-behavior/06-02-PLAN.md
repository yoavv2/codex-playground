---
phase: 06-live-updates-sse-and-reconnect-behavior
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - farfield/apps/mobile/src/live/event-routing.ts
  - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
  - farfield/apps/mobile/src/live/useLiveUpdates.ts
  - farfield/apps/mobile/app/_layout.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - `/events` messages are translated into targeted TanStack Query invalidations instead of full-screen polling behavior.
    - Thread list and active thread data refresh automatically from SSE triggers while avoiding refetch storms through debounce/coalescing.
    - Live-update infrastructure is mounted once at app root so screens consume shared state rather than opening independent SSE sockets.
  artifacts:
    - farfield/apps/mobile/src/live/event-routing.ts
    - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
    - farfield/apps/mobile/src/live/useLiveUpdates.ts
    - farfield/apps/mobile/app/_layout.tsx
  key_links:
    - farfield/apps/mobile/src/live/event-routing.ts classifies farfield `/events` payloads so farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx can invalidate only affected query keys.
    - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx uses query keys from farfield/apps/mobile/src/api/queryKeys.ts to invalidate threads/detail/approvals/modes consistently.
    - farfield/apps/mobile/app/_layout.tsx mounts the provider once so all tabs and the thread detail route share one live-update pipeline.
---

<objective>
Wire the Phase 06 data-sync core by mapping SSE events to query invalidation and app-wide live refresh behavior.

Purpose: Keep REST as the source of truth while making SSE the trigger path for fresh data.
Output: A root-mounted live-updates provider that performs debounced cache invalidation from `/events`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-01-SUMMARY.md
@farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
@farfield/apps/mobile/src/api/queryKeys.ts
@farfield/apps/mobile/src/hooks/useThread.ts
@farfield/apps/mobile/src/hooks/useThreads.ts
@farfield/apps/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create deterministic event-routing utilities for Farfield SSE payloads</name>
  <files>farfield/apps/mobile/src/live/event-routing.ts</files>
  <action>Implement a single parser/router module that reads typed SSE payloads from Plan 06-01 and emits actionable sync intents (for example: thread-list changed, specific thread changed, approvals changed, runtime-state changed). Prioritize server-produced metadata (`entry.meta.threadId`, action names/stages) and gracefully no-op on unrecognized payloads. Keep routing pure so behavior can be tested and reused by provider logic.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Incoming `/events` payloads can be mapped to specific invalidation intents without screen-specific parsing logic.</done>
</task>

<task type="auto">
  <name>Implement debounced query invalidation in the live-updates provider</name>
  <files>farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx</files>
  <action>Extend the provider to consume routed intents and invalidate relevant TanStack Query keys (`threads.list`, `threads.detail(threadId)`, `approvals.pending(threadId)`, and collaboration mode keys when applicable). Add per-domain debounce/coalescing so rapid history bursts (message deltas/tool updates) do not trigger excessive refetches. Keep REST endpoints canonical; do not build client-side event-reconstructed thread state.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Live data refresh happens automatically from SSE signals, and refetch frequency remains bounded under high event throughput.</done>
</task>

<task type="auto">
  <name>Expose a stable consumer hook and mount provider once at app root</name>
  <files>farfield/apps/mobile/src/live/useLiveUpdates.ts, farfield/apps/mobile/app/_layout.tsx</files>
  <action>Add a small `useLiveUpdates()` hook that exposes connection status and last-sync metadata from provider context. Mount `LiveUpdatesProvider` in `app/_layout.tsx` above navigation so tabs and thread detail share one connection and invalidation source. Ensure mounting order keeps `QueryClientProvider` available to live-update internals.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>The app has one shared live-update pipeline, and route components can read status via a stable hook.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` compiles with provider mounted in root layout.
- [ ] Manual smoke check against a secured local Farfield server confirms: starting/sending/interrupting a thread causes list/detail data to refresh via SSE-triggered invalidation without manual pull-to-refresh.
- [ ] Event bursts (for example active Codex turn output) do not cause runaway refetch loops.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SSE is used as invalidation trigger only, with REST remaining source of truth
- Automatic live refresh works for thread list and active thread during normal Codex activity
- One app-wide provider owns connection + invalidation to avoid duplicate SSE streams per screen
  </success_criteria>

<output>
After completion, create `.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-02-SUMMARY.md`
</output>
