---
phase: 06-live-updates-sse-and-reconnect-behavior
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/src/api/events.ts
  - farfield/apps/mobile/src/hooks/useFarfieldEvents.ts
  - farfield/apps/mobile/src/hooks/useSseConnection.ts
  - farfield/apps/mobile/src/settings/storage.ts
  - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - The mobile SSE transport exposes typed lifecycle/message/error signals and can run with reconnect control owned by app code instead of implicit library retry loops.
    - Live connection state supports bounded exponential backoff retries and foreground/background awareness so reconnect behavior is predictable on mobile.
    - Saved connection setting changes (URL/token) can trigger a live-update reconnect path without restarting the app.
  artifacts:
    - farfield/apps/mobile/src/api/events.ts
    - farfield/apps/mobile/src/hooks/useFarfieldEvents.ts
    - farfield/apps/mobile/src/hooks/useSseConnection.ts
    - farfield/apps/mobile/src/settings/storage.ts
    - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
  key_links:
    - farfield/apps/mobile/src/hooks/useSseConnection.ts builds on farfield/apps/mobile/src/hooks/useFarfieldEvents.ts and farfield/apps/mobile/src/api/events.ts to implement explicit retry/backoff.
    - farfield/apps/mobile/src/settings/storage.ts notifies farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx when server settings change so the SSE connection can rotate safely.
    - farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx exposes connection state for later query invalidation and UI work without coupling to route components.
---

<objective>
Establish the Phase 06 live-update foundation by making SSE connection lifecycle deterministic and app-aware.

Purpose: Build reliable reconnect primitives before wiring invalidation and UI behavior.
Output: A reusable live-updates provider plus connection hooks that can pause/resume and retry cleanly.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 06 context comes from roadmap + codebase (no 06-CONTEXT.md yet).
@farfield/apps/mobile/src/api/events.ts
@farfield/apps/mobile/src/hooks/useFarfieldEvents.ts
@farfield/apps/mobile/src/settings/storage.ts
@farfield/apps/mobile/app/_layout.tsx
@farfield/apps/server/src/index.ts
@farfield/node_modules/react-native-sse/src/EventSource.js
</context>

<tasks>

<task type="auto">
  <name>Harden the SSE transport contract for controlled reconnect behavior</name>
  <files>farfield/apps/mobile/src/api/events.ts</files>
  <action>Refactor `subscribeEvents()` to expose a richer, typed event contract for `/events` payloads (`state` and `history`) and transport errors. Add optional EventSource configuration passthrough (`pollingInterval`, `timeout`, `timeoutBeforeConnection`, debug) so callers can disable internal retry loops and own reconnection policy in React hooks. Keep Authorization-header auth as the primary path and preserve existing cleanup guarantees.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Callers can opt into fully controlled reconnect behavior and receive typed lifecycle/message/error callbacks from the SSE layer.</done>
</task>

<task type="auto">
  <name>Add a reusable AppState-aware SSE connection hook with bounded backoff</name>
  <files>farfield/apps/mobile/src/hooks/useFarfieldEvents.ts, farfield/apps/mobile/src/hooks/useSseConnection.ts</files>
  <action>Keep `useFarfieldEvents()` as the low-level subscription primitive, then add `useSseConnection()` that owns retry orchestration: explicit states (`idle`, `connecting`, `connected`, `reconnecting`, `paused`, `error`), capped exponential backoff with jitter, retry-at timestamp metadata, and AppState handling to pause reconnect churn in background and resume immediately on foreground. Ensure cleanup cancels timers/listeners to avoid duplicate sockets.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Any consumer can subscribe once and receive deterministic connection-state transitions plus retry metadata without implementing timer logic in screens.</done>
</task>

<task type="auto">
  <name>Create live-updates provider skeleton with settings-change awareness</name>
  <files>farfield/apps/mobile/src/settings/storage.ts, farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx</files>
  <action>Extend settings storage with a lightweight subscription mechanism so runtime changes to server URL/token can be observed by live-update infrastructure. Implement a provider that loads settings, subscribes to setting changes, drives `useSseConnection()`, and exposes connection status via React context. Do not wire query invalidation in this plan; this slice ends at transport + state distribution foundation.</action>
  <verify>`bun run --filter @farfield/mobile start -- --non-interactive --offline`</verify>
  <done>The app has one shared live-update context that tracks connection lifecycle and automatically reacts to saved settings changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` compiles with the new live-updates provider and hooks.
- [ ] Manual smoke check confirms: foreground/background transitions do not leak duplicate SSE connections, and retry delays increase then reset after a successful reconnect.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SSE reconnect behavior is explicit, bounded, and observable in code (not hidden in library defaults)
- Live-update connection state is available app-wide for Phase 06-02 invalidation wiring
- Settings edits can trigger reconnection behavior without killing the app process
  </success_criteria>

<output>
After completion, create `.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-01-SUMMARY.md`
</output>
