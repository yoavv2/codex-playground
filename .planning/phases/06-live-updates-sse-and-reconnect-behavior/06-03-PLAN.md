---
phase: 06-live-updates-sse-and-reconnect-behavior
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - farfield/apps/mobile/app/(tabs)/threads.tsx
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - farfield/apps/mobile/app/(tabs)/index.tsx
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can see live transport status (connected/reconnecting/disconnected/auth/config problems) directly in the UI while browsing and reading threads.
    - Thread and list screens remain usable when SSE drops, with manual refresh still available as the resilience fallback.
    - Phase 06 planning docs are closed out accurately after implementation and verification.
  artifacts:
    - farfield/apps/mobile/app/(tabs)/threads.tsx
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - farfield/apps/mobile/app/(tabs)/index.tsx
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - farfield/apps/mobile/app/(tabs)/threads.tsx and farfield/apps/mobile/app/thread/[threadId].tsx consume farfield/apps/mobile/src/live/useLiveUpdates.ts for transport-state copy and indicators.
    - The UI status layer complements (not replaces) pull-to-refresh controls already provided by farfield/apps/mobile/src/hooks/useThreads.ts and farfield/apps/mobile/src/hooks/useThread.ts.
    - Planning docs are updated only after live-update behavior is verified on device/simulator against a real secured Farfield server.
---

<objective>
Complete Phase 06 by surfacing reconnect/live-sync status in the mobile UI and finishing the planning handoff.

Purpose: Make live updates understandable to users while preserving manual fallback when SSE is unavailable.
Output: Threads/detail/connection screens reflecting live transport state, plus Phase 06 summary and roadmap/state updates.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-02-SUMMARY.md
@farfield/apps/mobile/src/live/useLiveUpdates.ts
@farfield/apps/mobile/app/(tabs)/threads.tsx
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/mobile/app/(tabs)/index.tsx
@farfield/apps/mobile/src/hooks/useThreads.ts
@farfield/apps/mobile/src/hooks/useThread.ts
</context>

<tasks>

<task type="auto">
  <name>Replace REST-only thread-list connection banner with live transport status</name>
  <files>farfield/apps/mobile/app/(tabs)/threads.tsx</files>
  <action>Update the Threads tab banner to incorporate live-update connection state from `useLiveUpdates()` while preserving existing REST-derived error classification for setup/auth failures. Add explicit reconnecting/disconnected copy that reflects retry progress (for example next retry delay) when available. Keep list behavior unchanged: pull-to-refresh still works and remains the fallback when SSE is down.</action>
  <verify>`bun run --filter @farfield/mobile typecheck`</verify>
  <done>Users can tell at a glance whether live updates are active, reconnecting, or unavailable without losing existing REST error clarity.</done>
</task>

<task type="auto">
  <name>Add thread-detail live-sync affordances without regressing chat usability</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx, farfield/apps/mobile/app/(tabs)/index.tsx</files>
  <action>Add a lightweight live-status row/chip in thread detail that reflects current SSE state and gives actionable fallback guidance when disconnected (for example "Pull to refresh while reconnecting"). Mirror the same connection signal in the Connection tab so setup troubleshooting and runtime status use a consistent vocabulary. Do not introduce markdown rendering, advanced diagnostics screens, or Phase 07 collaboration/user-input controls here.</action>
  <verify>`bun run --filter @farfield/mobile lint`</verify>
  <done>Thread-detail and connection surfaces clearly communicate live-sync health while keeping core chat/send/refresh flows stable.</done>
</task>

<task type="auto">
  <name>Close Phase 06 with summary and planning-state handoff</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>After implementation and manual verification, create `06-03-SUMMARY.md`, then update `.planning/STATE.md` and `.planning/ROADMAP.md` so Phase 06 is marked complete with correct plan/summary counts and the next action points to Phase 07. Keep history additions concise and factual, preserving earlier records unchanged.</action>
  <verify>`test -f .planning/phases/06-live-updates-sse-and-reconnect-behavior/06-03-SUMMARY.md`</verify>
  <done>Planning docs reflect completion of live-update/reconnect scope and cleanly hand off to Phase 07.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] `bun run --filter @farfield/mobile lint` passes.
- [ ] `bun run --filter @farfield/mobile start -- --non-interactive --offline` starts with new live-status UI compiled.
- [ ] Manual smoke check on simulator/device against a secured local Farfield server verifies:
- [ ] Active thread updates while Codex is running (without manual refresh).
- [ ] Temporarily dropping network (or killing server) shows reconnect/disconnected status, then recovers automatically when connectivity returns.
- [ ] Thread list and detail remain usable with manual pull-to-refresh fallback during disconnect windows.
- [ ] `.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-03-SUMMARY.md` exists and `.planning/STATE.md` + `.planning/ROADMAP.md` are updated for Phase 06 completion.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live transport state is visible and understandable in the MVP UI
- Reconnect behavior is resilient but non-blocking, with manual refresh fallback preserved
- Planning artifacts cleanly transition work from Phase 06 to Phase 07
  </success_criteria>

<output>
After completion, create `.planning/phases/06-live-updates-sse-and-reconnect-behavior/06-03-SUMMARY.md`
</output>
