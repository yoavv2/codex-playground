---
phase: 08-ux-polish-and-platform-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/package.json
  - farfield/apps/mobile/app/thread/[threadId].tsx
  - farfield/apps/mobile/src/components/thread/MessageMarkdown.tsx
  - farfield/apps/mobile/src/components/thread/ApprovalActionCard.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Thread conversation content renders markdown and fenced code blocks clearly on iOS and Android.
    - Users can copy message/code content directly from thread detail.
    - Pending approvals can be approved or denied from mobile with clear per-request pending/error feedback.
    - Composer send behavior feels immediate while remaining consistent with server-refetched thread state.
  artifacts:
    - farfield/apps/mobile/package.json
    - farfield/apps/mobile/app/thread/[threadId].tsx
    - farfield/apps/mobile/src/components/thread/MessageMarkdown.tsx
    - farfield/apps/mobile/src/components/thread/ApprovalActionCard.tsx
  key_links:
    - farfield/apps/mobile/app/thread/[threadId].tsx renders turn text through farfield/apps/mobile/src/components/thread/MessageMarkdown.tsx.
    - Approval cards in farfield/apps/mobile/app/thread/[threadId].tsx invoke useRespondToApproval() from farfield/apps/mobile/src/hooks/useThreadMutations.ts.
    - Copy actions in thread detail route through expo clipboard APIs declared in farfield/apps/mobile/package.json.
---

<objective>
Polish thread detail interactions for day-to-day remote usage.

Purpose: Make conversation reading/responding faster and safer on phone while finishing approval decision actions.
Output: Markdown/code rendering, copy controls, approval approve/deny actions, and improved composer behavior.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-collaboration-mode-and-user-input-requests/07-03-SUMMARY.md
@farfield/apps/mobile/app/thread/[threadId].tsx
@farfield/apps/mobile/src/hooks/useThreadMutations.ts
@farfield/apps/mobile/src/api/approvals.ts
@farfield/apps/mobile/src/api/thread-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Add markdown/code rendering with copy affordances for conversation turns</name>
  <files>farfield/apps/mobile/package.json, farfield/apps/mobile/app/thread/[threadId].tsx, farfield/apps/mobile/src/components/thread/MessageMarkdown.tsx</files>
  <action>Add mobile-safe markdown rendering for turn text (including fenced code blocks) and wire copy controls for whole message and code blocks. Keep strict typing and avoid ad-hoc parsing in the route file by introducing a focused renderer component.</action>
  <verify>cd farfield && bun run --filter @farfield/mobile typecheck</verify>
  <done>Turns render with readable markdown/code formatting and copy interactions on device/simulator.</done>
</task>

<task type="auto">
  <name>Wire pending approval approve/deny actions in thread detail</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx, farfield/apps/mobile/src/components/thread/ApprovalActionCard.tsx</files>
  <action>Replace read-only pending approval cards with actionable approve/deny controls using useRespondToApproval(). Add per-request pending state, disable duplicate taps during mutation, and show inline error text when submission fails.</action>
  <verify>cd farfield && bun run --filter @farfield/mobile lint</verify>
  <done>Users can resolve command/file-change/apply-patch approvals directly from phone.</done>
</task>

<task type="auto">
  <name>Improve composer responsiveness and error/empty-state polish in thread detail</name>
  <files>farfield/apps/mobile/app/thread/[threadId].tsx</files>
  <action>Refine composer flow so send interaction is immediate, duplicate sends are prevented, and failure restores clear actionable state. Keep list/header sections readable when conversation is empty or when requests/approvals are absent.</action>
  <verify>cd farfield && CI=1 bun run --filter @farfield/mobile start -- --offline</verify>
  <done>Thread detail remains readable and responsive through send, error, and empty-state paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] In `farfield/`, `bun run --filter @farfield/mobile lint` passes.
- [ ] Manual thread-detail smoke check confirms markdown/code rendering and copy actions work.
- [ ] Manual smoke check confirms approve/deny controls resolve pending approvals and refresh UI state.
- [ ] Composer interaction remains stable (no duplicate sends, clear pending/error behavior).
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Thread detail UX is materially improved for reading/responding from phone
- Approval actions are no longer read-only in mobile UI
  </success_criteria>

<output>
After completion, create `.planning/phases/08-ux-polish-and-platform-readiness/08-01-SUMMARY.md`
</output>
