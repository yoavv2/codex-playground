---
phase: 08-ux-polish-and-platform-readiness
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - farfield/apps/mobile/src/settings/types.ts
  - farfield/apps/mobile/src/settings/storage.ts
  - farfield/apps/mobile/src/settings/index.ts
  - farfield/apps/mobile/app/(tabs)/settings.tsx
  - farfield/apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - App supports two preset profiles (Local and Tailscale) with separate URL/token values.
    - Switching active profile updates Connection/health behavior without app restart.
    - Existing single-profile stored data is migrated into the new profile model without data loss.
  artifacts:
    - farfield/apps/mobile/src/settings/types.ts
    - farfield/apps/mobile/src/settings/storage.ts
    - farfield/apps/mobile/src/settings/index.ts
    - farfield/apps/mobile/app/(tabs)/settings.tsx
    - farfield/apps/mobile/app/(tabs)/index.tsx
  key_links:
    - farfield/apps/mobile/app/(tabs)/settings.tsx updates profile state through farfield/apps/mobile/src/settings/storage.ts notify APIs.
    - farfield/apps/mobile/app/(tabs)/index.tsx reflects and tests the active profile loaded via settings module.
    - save notifications from settings storage are consumed by live updates connection lifecycle.
---

<objective>
Add preset-based profile switching for Local/Tailscale connections.

Purpose: Remove repetitive manual re-entry when moving between local and remote usage contexts.
Output: Typed profile-state model, migrated storage, and profile-switching UI in Settings/Connection.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-collaboration-mode-and-user-input-requests/07-03-SUMMARY.md
@farfield/apps/mobile/src/settings/types.ts
@farfield/apps/mobile/src/settings/storage.ts
@farfield/apps/mobile/app/(tabs)/settings.tsx
@farfield/apps/mobile/app/(tabs)/index.tsx
@farfield/apps/mobile/src/live/LiveUpdatesProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Introduce typed Local/Tailscale profile state with migration from existing settings keys</name>
  <files>farfield/apps/mobile/src/settings/types.ts, farfield/apps/mobile/src/settings/storage.ts, farfield/apps/mobile/src/settings/index.ts</files>
  <action>Add explicit profile-state types and storage helpers for two preset profiles (`local`, `tailscale`) plus `activeProfileId`. Implement one-time migration that maps current single-profile values into `local` and initializes `tailscale` as empty. Preserve strict token handling via secure storage paths per profile id.</action>
  <verify>cd farfield && bun run --filter @farfield/mobile typecheck</verify>
  <done>Profile state is versioned, typed, and migration-safe with deterministic active-profile behavior.</done>
</task>

<task type="auto">
  <name>Build Settings preset switcher and per-profile editor</name>
  <files>farfield/apps/mobile/app/(tabs)/settings.tsx</files>
  <action>Add a segmented/pill switcher for Local vs Tailscale, show/edit server URL and token for the selected preset, and persist edits through profile-aware storage APIs. Keep validation behavior clear and preserve save feedback.</action>
  <verify>cd farfield && bun run --filter @farfield/mobile lint</verify>
  <done>Users can maintain distinct Local/Tailscale credentials and switch active preset from Settings.</done>
</task>

<task type="auto">
  <name>Reflect active profile in Connection tab and health checks</name>
  <files>farfield/apps/mobile/app/(tabs)/index.tsx</files>
  <action>Display active profile label in the Connection tab and ensure Test Connection uses the currently active profile values. Keep live transport status and health messaging coherent after profile switches.</action>
  <verify>cd farfield && CI=1 bun run --filter @farfield/mobile start -- --offline</verify>
  <done>Profile switching immediately affects connection diagnostics and runtime behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] In `farfield/`, `bun run --filter @farfield/mobile typecheck` passes.
- [ ] In `farfield/`, `bun run --filter @farfield/mobile lint` passes.
- [ ] Manual check: save different URLs/tokens for Local and Tailscale, switch between them, and confirm values persist.
- [ ] Manual check: Connection tab shows active profile identity and health test targets the selected profile.
- [ ] Live updates reconnect behavior remains stable after profile switch and save.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Profile switching removes manual reconfiguration friction for local vs remote operation
- Existing installations retain previously saved values via migration
  </success_criteria>

<output>
After completion, create `.planning/phases/08-ux-polish-and-platform-readiness/08-02-SUMMARY.md`
</output>
